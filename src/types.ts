export type TUserDB = {
    id: string,
    name: string,
    email: string,
    password: string
}

export type TTaskDB = {
    id: string,
    title: string,
    description: string,
    created_at: string,
    status: number
}